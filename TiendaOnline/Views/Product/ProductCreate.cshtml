@model TiendaOnline.Models.Dto.ProductDto
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@section Styles {
    <link rel="stylesheet" href="~/css/product-style.css" />
}
<div class="product-form-container">
    <h2 class="poduct-form-title">Crear Nuevo Producto</h2>
    <form asp-action="ProductCreate" enctype="multipart/form-data">
        <div class="form-group">
            <label asp-for="Name" class="form-label">Nombre del Producto</label>
            <input asp-for="Name" class="form-control form-control-sm" placeholder="Ingrese el nombre del producto" />
            <span asp-validation-for="Name" class="validation-message"></span>
        </div>

        <div class="form-group">
            <label asp-for="CategoryName" class="form-label">Categoria</label>
            <input asp-for="CategoryName" class="form-control form-control-sm" placeholder="Seleccione una categoria" />
            <span asp-validation-for="CategoryName" class="validation-message"></span>
        </div>

        <div class="form-group">
            <label asp-for="Description" class="form-label">Descripcion</label>
            <textarea asp-for="Description" class="form-control form-control-sm" placeholder="Describa el producto detalladamente" />
            <span asp-validation-for="Description" class="validation-message"></span>
        </div>

        <div class="form-group">
            <label asp-for="Price" class="form-label">Precio</label>
            <div class="input-group">
                <spam class="input-group-text">$</spam>
                <input asp-for="Price" class="form-control form-control-sm w-75" placeholder="0.00" type="number" step="0.01" min="0" />
            </div>
            <span asp-validation-for="Price" class="validation-message"></span>
        </div>

        <div class="form-group">
            <label asp-for="Image" class="form-label">Imagen del producto</label>
            <div class="file-input-container">
                <input asp-for="Image" class="form-control form-control-sm" type="file" accept="image/*" />
                <input asp-for="ImageUrl" hidden class="form-control" />
            </div>
            <span asp-validation-for="Image" class="validation-message"></span>
        </div>

        <div class="form-actions">
            <a asp-actions="ProductIndex" class="btn btn-primary">Volver al Listado</a>
            <button type="submit" class="btn btn-submit" style="color:#fff;">Crear Producto</button>
        </div>
    </form>
</div>
<script>
    document.querySelector('input[type="file"]').addEventListener('change', function (e) {
        const fileName = e.target.files[0]?.name || 'Ningun archivo seleccionado';
        const fileInputLabel = this.nextElementSilbling;
        if (fileInputLabel && fileInputLabel.classList.contains('file-input-label')) {
            const fileNameElement = fileInputLabel.querySelector('.file-name');
            if (fileNameElement){
                fileNameElement.textContent = fileName;
            }
        }
    });
</script>
@section Scripts{
    <partial name="_ValidationScriptsPartial" />
}